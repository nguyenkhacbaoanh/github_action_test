1.  Create the Python files defining an example application.

2. Bundle the example files into a Python wheel.

3. Create a job to run the Python wheel.

4. Run the job and view the results.


#1 Create the Python files defining an example application.

```bash
pip install wheel setuptools
```